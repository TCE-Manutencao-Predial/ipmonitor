services:
  ipmonitor:
    build:
      context: .
      dockerfile: Dockerfile
    image: ipmonitor:1.0.0
    container_name: ipmonitor
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ipmonitor-data:/var/softwaresTCE/dados/ipmonitor
    networks:
      - multiplicador_network

volumes:
  ipmonitor-data:
    name: ipmonitor-data

networks:
  multiplicador_network:
    external: true
