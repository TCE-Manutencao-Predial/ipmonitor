# ============================================================================
# Configuração de Deploy - IP Monitor
# ============================================================================
# Este arquivo é um TEMPLATE. Copie para .env.deploy e ajuste os valores.
# IMPORTANTE: .env.deploy é a fonte única de verdade para configurações!
# ============================================================================

# ----------------------------------------------------------------------------
# IDENTIFICAÇÃO DO PROJETO
# ----------------------------------------------------------------------------
PROJECT_NAME=ipmonitor
PROJECT_NAME_DISPLAY="IP Monitor"
PROJECT_NAME_GIT=ip-monitor
SERVICE_NAME=ipmonitor

# ----------------------------------------------------------------------------
# REPOSITÓRIO GIT
# ----------------------------------------------------------------------------
GIT_REPO_NAME=ip-monitor
GIT_REPO_OWNER=TCE-Manutencao-Predial

# ----------------------------------------------------------------------------
# DOMÍNIO E ROTAS
# ----------------------------------------------------------------------------
DOMAIN_BASE=automacao.tce.go.gov.br
ROUTES_PREFIX=/ipmonitor

# ----------------------------------------------------------------------------
# CAMINHOS BASE (Linux Production)
# ----------------------------------------------------------------------------
BACKEND_ROOT=/var/softwaresTCE
FRONTEND_ROOT=/var/www/automacao.tce.go.gov.br
DATA_ROOT=/var/softwaresTCE/dados
LOGS_ROOT=/var/softwaresTCE/logs

# ----------------------------------------------------------------------------
# PORTA DO SERVIÇO
# ----------------------------------------------------------------------------
PORT=8000

# ----------------------------------------------------------------------------
# REDE (VLAN)
# ----------------------------------------------------------------------------
NETWORK_BASE=172.17.{vlan}.

# ----------------------------------------------------------------------------
# OPÇÕES DE DEPLOY
# ----------------------------------------------------------------------------
AUTO_ENABLE_SERVICE=true
CREATE_SYMLINKS=true

# ----------------------------------------------------------------------------
# CAMINHOS DERIVADOS (Calculados automaticamente pelos scripts)
# ----------------------------------------------------------------------------
# ROOT_BACKEND=${BACKEND_ROOT}/${PROJECT_NAME}
# ROOT_FRONTEND=${FRONTEND_ROOT}/${PROJECT_NAME}
# ROOT_DATA=${DATA_ROOT}/${PROJECT_NAME}
# ROOT_LOGS=${LOGS_ROOT}/${PROJECT_NAME}
# GIT_REPO_URL=https://github.com/${GIT_REPO_OWNER}/${GIT_REPO_NAME}.git
